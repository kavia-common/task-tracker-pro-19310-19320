# Base URL for the backend REST API
# Example: http://localhost:8000 or https://api.example.com
NEXT_PUBLIC_API_BASE_URL=

# Optional: used by mock auth as a static fallback secret (not secure, for demo)
NEXT_PUBLIC_DEMO_AUTH_SECRET=demo-secret
